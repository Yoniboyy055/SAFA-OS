{
  "name": "jarvis-os-governed",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "jarvis": "dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli/index.js",
    "test:runner": "node --test dist/tests/*.test.js",
    "test": "npm run build && npm run test:runner"
  },
  "devDependencies": {
    "typescript": "latest"
  },
  "dependencies": {
    "nodemailer": "latest"
  }
}

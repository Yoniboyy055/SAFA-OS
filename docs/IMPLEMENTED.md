# Implemented

- Governor enforcement (approvals, kill switch, strict mode).
- Audit logging with redaction and append-only behavior.
- Local skills: file ops, search, run_tests, memory tiers.
- Network corridor with allowlist + approval gates and optional live mode.
- Outbound preview modules (email, payments, calls).
- LLM router (registry, policy, providers) with live call gates.
- Dashboard server and UI (local-only) with approvals and audit views.
- Persistence stores (approvals, tasks, settings, vault layout).
- Skill receipts with hashing and audit trail.
- Runtime daemon (dashboard + health endpoint).
- CLI plan/run/exec workflows with approval plumbing.

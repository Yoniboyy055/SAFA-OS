# Freeze Control (One-Button Safety)

Freeze halts:
- Task runner
- Automation workflows
- External interaction paths

When freeze is enabled, governor denies all actions except:
- status / skills
- freeze / unfreeze controls

State is persisted in `data/control/freeze.json`.
